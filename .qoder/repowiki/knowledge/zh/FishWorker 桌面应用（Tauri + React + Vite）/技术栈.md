React 19 + TypeScript 5.8 + Vite 7 + Vitest(jsdom)；桌面壳层用 Tauri v2（`@tauri-apps/api`、`plugin-opener`、`plugin-window-state`）；UI 采用 Arco Design + Radix Popover/DropdownMenu + Floating UI；富文本基于 TiPTAP 3 Starter Kit 并自定义 blockquote/code-block/image/list 节点；拖拽使用 @dnd-kit；状态管理选用 Zustand；思维导图使用 simple-mind-map（通过 npm stub 替换 quill 依赖）。