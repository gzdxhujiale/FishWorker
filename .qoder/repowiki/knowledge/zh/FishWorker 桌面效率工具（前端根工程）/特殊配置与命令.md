开发需安装 Tauri CLI 后执行 `pnpm tauri:dev`（内部调用 `vite` 监听 1420 端口）；构建产物由 `tsc -b && vite build` 生成；测试命令为 `pnpm test` / `pnpm test:watch`。项目通过 `pnpm.overrides` 与 `overrides.quill=link:./scripts/npm-stubs/quill` 强制把 `quill` 指向本地 stub，否则 `simple-mind-map` 会引入不兼容版本。