# 产品需求文档 (PRD) - 清单模块 v1.9

## 1. 概述
### 包含:
- **问题陈述：** 用户需要一个专门的模块来创建、分类和管理各种待办清单或笔记，以便于更好地组织个人任务和信息，提升个人时间管理和信息检索的效率。
- **建议的解决方案：** 新增“清单”功能页面，采用经典的左侧导航+右侧内容的页面结构，支持清单的快捷创建、个性化属性配置（如图标、颜色、视图等）以及通过文件夹进行层级化管理。引入快捷悬浮菜单、笔记抽屉式编辑和模板功能，提升内容记录体验。

## 2. 核心理念
通过灵活的清单分类、视觉化的高亮配置（颜色和图标）以及无缝的交互体验，帮助用户构建结构化、个性化的个人待办与知识管理体系。

## 3. 解决方案概述

### 3.1 建议的解决方案
- **高层级描述**: 构建一个全新的“清单”页面，包含左侧侧边栏（导航）和右侧主内容区。侧边栏用于展示所有清单及文件夹树形结构；右侧用于展示当前选中清单的详细内容。右侧内容区支持笔记的快速录入（输入框回车），并通过右侧滑出的抽屉面板进行详细编辑。
- **核心能力**: 清单的创建与管理、文件夹层级管理、清单属性自定义、快捷悬浮操作菜单、笔记的抽屉式编辑、笔记模板库。

### 3.2 包含在本次范围内
- **功能点 1：清单页面基础框架** - 实现左右分栏布局结构。
- **功能点 2：左侧导航栏展示及交互** - 清单列表展示、文件夹层级嵌套展示、hover 标题显示添加按钮。
- **功能点 3：侧边栏更多操作菜单** - 鼠标悬停文件夹或清单时显示“...”更多菜单，支持编辑、置顶、复制、删除等快捷操作，并规范了对应弹窗的一致性。
- **功能点 4：“添加/编辑清单”与“文件夹”弹窗** - 支持设置清单名称、图标、颜色、视图类型（列表/看板等），统一添加与编辑的 UI 样式。
- **功能点 5：内联文件夹管理** - 支持在创建清单时下拉选择已有文件夹，或直接在下拉菜单中新建文件夹。
- **功能点 6：笔记管理与抽屉编辑** - 清单内容区顶部提供笔记录入框（回车创建），笔记列表支持 Hover 操作菜单（置顶、移动到、复制、删除）。点击笔记可唤出右侧抽屉进行标题和内容的详细编辑。
- **功能点 7：笔记模板引导** - 清单为空时提供引导，完整显示“记录你的想法，或 使用模板”，支持点击弹出“笔记模板”弹窗选择并应用模板。
- **功能点 8：自定义确认与列表拖拽** - 采用自定义气泡确认框替代系统原生确认，清单支持拖拽排序。
- **功能点 9：清单内部分组管理** - 清单内容区支持创建分组，将笔记进行分类管理，支持分组折叠与安全删除。
- **功能点 10：笔记模板管理与应用优化** - 鼠标悬浮笔记模板时，右上角浮现编辑和删除按钮，支持管理模板库；应用模板时仅覆盖内容，保留原笔记标题。
- **功能点 11：拖拽交互视觉增强** - 在拖拽清单到文件夹或拖拽笔记到分组时，目标区域增加明显的视觉提示（如高亮背景），提升用户对放置位置的感知。
- **功能点 12：富文本编辑器升级 (TipTap)** - 将笔记编辑面板与模板编辑面板的普通纯文本输入框升级为 TipTap 富文本编辑器，支持 Markdown 语法快捷转换、富文本排版、代码块和引用块。
- **功能点 13：Markdown 导入与导出** - 在笔记详情下拉菜单中添加“导入MD”与“导出MD”选项。通过 Tauri 调用后端的原生系统文件对话框（rfd）直接在本地读写 `.md` 格式文件，绕过 WebView2 对 Blob 下载的安全限制。
- **功能点 14：批量导入 MD** - 支持在清单主内容区的右上角“...”下拉菜单中选择批量导入，多选本地 `.md` 文件，解析文件名作为笔记标题，内容作为笔记正文批量新建笔记。
- **功能点 15：批量导出 MD** - 支持在下拉菜单中选择批量导出，弹出笔记多选弹窗，支持全选/多选当前清单下的笔记，点击确认后由后端调起保存文件夹选择器，将选中的笔记全部导出为独立的 `.md` 文件。
- **功能点 16：全局 Toast 提示系统** - 导入、导出、批量导入、批量导出及其他异步核心操作完成后，提供顶层悬浮、自动渐隐的全局 Toast 消息提示。

### 3.3 超出本次范围
- 多级嵌套文件夹（目前仅支持单层文件夹分类，即文件夹下直接是清单）。

## 4. 用户故事与需求

### 4.1 用户故事
```
作为一名 需要管理多项工作和生活事务的用户
我希望 能够在侧边栏快捷编辑或置顶我常用的清单和文件夹
以便于 我能更高效地访问最重要的内容

作为一名 经常记录会议或周报的用户
我希望 在新建笔记时能直接使用模板
以便于 统一格式，节省排版时间
```

### 4.2 功能需求
| ID | 需求描述 | 备注 |
|----|------------|-------|
| FR1 | 用户可以访问“清单”页面，分为左右两部分。左侧清单被选中时应有轻微高亮背景提示当前激活状态。 | 核心布局与状态 |
| FR2 | 左侧侧边栏最上方显示“清单”分类标签，鼠标悬停（Hover）到该标签时，右侧显示“+”加号按钮。 | 交互细节 |
| FR3 | 点击“+”按钮，弹出“添加清单” Modal 弹窗。 | 核心交互 |
| FR4 | 在“添加清单”弹窗中，包含表单项：名称、颜色、视图类型、文件夹下拉框。 | 必填项：名称 |
| FR5 | 文件夹下拉框支持选择“无”或已有文件夹。底部固定“+ 添加文件夹”入口，支持新建并选中。 | 快捷操作 |
| FR6 | 左侧侧边栏中的文件夹支持点击折叠/展开操作。 | 树形结构展示 |
| FR7 | **鼠标移动到侧边栏文件夹时，右侧出现“...”按钮，点击展开菜单，包含：添加清单、编辑、置顶。**<br/>- *添加清单*：弹出添加清单弹窗，**默认选中该文件夹，赋予该文件夹的属性**。<br/>- *编辑*：**弹出重命名文件夹弹窗（弹窗样式需跟之前创建文件夹的弹窗样式保持一致）**。<br/>- *置顶*：将该文件夹固定在列表顶部。 | v1.2 明确交互 |
| FR8 | **侧边栏清单右侧默认显示数字，该数字表示该清单下包含多少条笔记。鼠标移动到清单上时，数字隐藏并变为“...”菜单按钮。** 点击展开菜单包含：编辑、置顶、复制、删除。<br/>- *编辑*：弹出编辑清单弹窗，样式同添加清单。 | v1.4 修改 |
| FR9 | **清单内容区上方，添加笔记的交互方式为：展示一个输入框，用户输入文本并按回车键（Enter）才会创建一条新笔记。** | v1.2 修改交互 |
| FR10 | **点击任一已创建的笔记，从界面右侧滑出抽屉（Drawer）面板。在抽屉中可修改编辑笔记标题和内容。点击抽屉式面板外部区域可直接关闭面板。** | v1.2 明确交互 |
| FR11 | **笔记右侧抽屉的右下角有“...”菜单按钮，点击展开包含：置顶、创建副本、保存为模板、删除。** | 核心交互 |
| FR12 | **当当前笔记内容为空时，笔记内容第一行展示完整的提示文本：“记录你的想法，或 使用模板”。其中“使用模板”可点击，点击后弹出“笔记模板”弹窗。** | v1.2 完善描述 |
| FR13 | **点击“使用模板”，弹出“笔记模板”弹窗，提供多种预设排版（如会议纪要、阅读笔记、周报等）供用户选择。** | 核心交互 |
| FR14 | **删除清单时，使用自定义的悬浮气泡确认框进行二次确认。删除笔记和分组不需要确认，直接删除。** | v1.4 修改 |
| FR15 | **在侧边栏复制清单时，必须将其内部包含的所有笔记数据也一并复制。** | v1.3 新增 |
| FR16 | **侧边栏的清单支持鼠标拖拽移动（Drag & Drop）来重新排序。文件夹内的清单可以拖出文件夹，文件夹外的清单也可以拖进文件夹内，并自动更新对应的 folderId 值。** | v1.4 修改 |
| FR17 | **鼠标移动到清单内容区的笔记列表项时，笔记右侧浮现下拉菜单按钮。点击后弹出菜单：置顶、移动到（二级菜单，展示其他清单，支持搜索）、创建副本、删除。** | v1.3 新增 |
| FR18 | **新建的清单默认无分组，归入【未分组】。未分组不可修改组名、不可删除。**<br/>- 点击右上角“...”创建分组。<br/>- **分组栏最右侧显示该分组下的笔记数量，鼠标移动到分组时，数字变为下拉菜单按钮（包含重命名、删除）。**<br/>- **删除分组：直接删除分组本身且无确认提示，原笔记自动归入【未分组】。**<br/>- 分组支持折叠和展开。 | v1.4 修改 |
| FR19 | **笔记支持拖拽排序（通过新增连续整数型字段），并支持拖拽改变所属分组（改变 groupId）。** | v1.4 新增 |
| FR20 | **鼠标移动到各个笔记模板上时，hover时右上角出现编辑和删除的按钮。支持点击以编辑和删除对应的笔记模板。** | v1.5 新增 |
| FR21 | **应用笔记模板时，系统只修改笔记内容，不修改当前的笔记标题。笔记模板的标题只是表明这个笔记模板的大览标识。** | v1.5 新增 |
| FR22 | **当拖动清单悬浮在某个文件夹上方时，该文件夹应有视觉反馈（如高亮背景），表示松手后将移入该文件夹。** | v1.6 新增 |
| FR23 | **当拖动笔记悬浮在某个分组上方时，该分组应有同样的视觉反馈。** | v1.6 新增 |
| FR24 | **笔记编辑和模板编辑全面升级为 TipTap 富文本编辑器。输入内容高度自适应，隐藏内部滚动条以避免双滚动条冲突。** | v1.7 新增 |
| FR25 | **笔记模板内置格式转换为 HTML 富文本，当应用模板或导入旧版文本时，系统在后台对纯文本自动转换段落标签，保证格式不混乱。** | v1.7 新增 |
| FR26 | **笔记提供原生 Markdown 文件导入与导出功能，调用系统原生的文件选择与保存对话框完成。** | v1.7 新增 |
| FR27 | **清单主内容区右上角操作下拉菜单（“...”）新增“批量导入MD”和“批量导出MD”两个操作项。** | v1.8 新增 |
| FR28 | **点击“批量导入MD”时，唤起原生多选文件对话框（rfd::pick_files），支持多选本地 `.md` 格式文件进行批量导入。以每个文件的文件名（去除 `.md` 后缀）作为新建笔记的标题，以文件内容（转换为 HTML）作为笔记正文写入，批量添加到当前清单下并同步持久化。** | v1.8 新增 |
| FR29 | **点击“批量导出MD”时，弹出“批量导出笔记”弹窗，平铺展示当前清单下所有的笔记列表（每项前带 Checkbox 勾选框，弹窗顶部提供“全选”复选框）。** | v1.8 新增 |
| FR30 | **用户在弹窗中勾选笔记并点击“确认导出”后，弹窗关闭并调起原生文件夹选择对话框（rfd::pick_folder）。确认保存的目录后，后端将所有勾选笔记的富文本内容转换为 Markdown 文本，并批量在该目录下以 `{笔记标题}.md` 文件名写入保存。自动过滤文件名中的非法字符，同名冲突时追加数字区分。** | v1.8 新增 |
| FR31 | **对于导入、导出、批量导入、批量导出及“保存为模板”等异步/弹出交互操作，操作成功后需在屏幕上方居中位置滑出短暂悬浮的全局提示（Toast Message）。** | v1.9 新增 |
| FR32 | **Toast 提示需具有状态类别（如代表成功的绿/靛色，代表出错的红色），并搭配相应的图标以增强视觉表现力。** | v1.9 新增 |
| FR33 | **Toast 提示不应阻塞用户其他交互操作，且应在显示 3 秒后自动渐隐关闭。** | v1.9 新增 |



### 4.3 非功能需求
- **易用性 (Usability)：** 侧边栏 Hover 菜单响应需迅速；右侧抽屉滑出动画需流畅平滑，抽屉外点击关闭符合用户直觉。
- **UI/UX：** 需高度还原设计稿，保持添加/编辑弹窗（清单、文件夹）的样式一致性；空白引导页中文案的点击区域需明显（如添加下划线或高亮颜色）。

## 5. 设计与用户体验

### 5.1 设计原则
- **上下文内操作：** 尽可能在用户光标悬停的地方提供相关操作（Hover 菜单），减少鼠标移动距离。
- **平滑过渡：** 笔记编辑采用抽屉模式而非页面跳转，帮助用户保持在当前清单上下文中的感知。
- **统一性：** “添加”与“编辑”的弹窗组件应尽可能复用，保证用户体验的一致性。

### 5.2 页面结构与交互流程
- **页面布局：** 
  - **左侧边栏**：清单导航，支持清单拖拽排序。
  - **中/右侧内容区**：顶部为笔记快捷输入框，下方为清单内的笔记列表（按分组展示，默认为无分组/未分组）。
  - **右侧抽屉 (Drawer)**：点击笔记时浮现于最右侧或覆盖内容区右半部，用于笔记编辑。点击其外部遮罩层关闭。
- **交互流程（更新）：**
  1. 笔记移动与排序：支持直接拖拽笔记以调整顺序或将其移入其他分组。也可通过 Hover 菜单将笔记跨清单转移。
  2. 分组管理：未分组不可改名或删除。分组标题最右侧显示笔记数，Hover 时变为下拉菜单。删除分组时无需确认，笔记自动归入“未分组”。
  3. 清单拖拽：清单支持在侧边栏中自由拖拽排序，支持拖出或拖入文件夹。

### 5.3 信息架构
- 侧边栏
  - 文件夹 (Hover: 添加清单(带默认文件夹)/编辑文件夹/置顶)
    - 清单 (右侧显示笔记数, Hover 变为菜单: 编辑/置顶/复制/删除, 支持拖拽进出文件夹)
- 主内容区
  - 顶部操作区: [新建笔记输入框...] / 更多操作“...” (创建分组)
  - 分组 A (最右侧显示笔记数, Hover 变为菜单: 重命名/删除, 可折叠)
    - 笔记项 1 (支持拖拽排序和改组, Hover: 置顶/移动到/创建副本/删除)
  - [未分组] (不可重命名/删除)
    - 笔记项 2

## 6. 技术规格说明建议

### 6.1 前端组件划分
- `ListLayout`, `Sidebar`, `AddListModal`, `FolderModal`
- **新增/更新组件**:
  - `HoverContextMenu`: 通用的 Hover 更多操作下拉菜单组件。
  - `ConfirmBubble`: 悬浮确认气泡框组件，用于替换 `window.confirm`。
  - `NoteGroup`: 清单内容区的分组容器组件。
  - `NoteItem`: 独立的笔记项组件，包含 Hover 时的下拉菜单和移动至搜索面板。
  - `NoteList`: 清单主内容区的笔记列表组件。
  - `NoteDrawer`: 右侧滑出的抽屉组件（集成 TipTap 富文本编辑）。

### 6.2 数据模型定义 (参考)
- **List Entity**: 
  - `id`: string, `name`: string, `folderId`: string | null, `isPinned`: boolean
  - `sortOrder`: number (连续型整数，用于拖拽排序)
- **NoteGroup Entity** (新增):
  - `id`: string
  - `listId`: string
  - `name`: string
  - `sortOrder`: number
- **Note Entity**:
  - `id`: string
  - `listId`: string
  - `groupId`: string | null (归属的分组，null 为未分组)
  - `sortOrder`: number (连续型整数，用于拖拽排序)
  - `title`: string
  - `content`: string (HTML 富文本格式)
  - `isPinned`: boolean
  - `createdAt`, `updatedAt`: timestamp
- **Template Entity**:
  - `id`: string
  - `name`: string
  - `content`: string (HTML 富文本格式)

### 6.3 数据持久化与初始化优化 (v1.7 更新)
清单模块的数据采用 **Rust + TiDB 直连** 的方案进行持久化同步，并在 v1.7 版本中对连接健壮性与初始化性能进行了关键性优化：
- **直连配置硬编码**: 为保障打包后应用缺失 `mysql.config.json` 时能开箱即用，将数据库连接的各项缺省值（Host、Port、User、Password、Database）以 Fallback 形式硬编码在 Rust 代码中。
- **SSL 加密与超时策略**: 连接 URL 结尾追加了 `?ssl-mode=required`，强制开启 TLS 安全加密通信以满足 TiDB Cloud 安全审计要求，同时配置 `acquire_timeout(10s)` 避免因异地跨网络连接超时导致启动中断。
- **延迟/后台异步初始化 (White Screen Fix)**: 将原本阻塞 Tauri 启动主进程（`.setup`）的同步数据库连接握手修改为 `connect_lazy`。连接池初始化立即返回，而表结构检测（`ensure_tables`）在后台 Tokio 线程中异步执行，彻底消除了程序打开时产生白屏数秒的问题。
- **高频输入防抖 (Debouncing)**: 对高频次的编辑引入 `500ms` 的延迟防抖，对低频次的编辑引入 `300ms` 的防抖，避免极短时间内触发过多的 IPC 调用和数据库写入操作。
- **数据表设计**: 所有数据分为 `list_folders`、`list_lists`、`list_note_groups`、`list_notes`、`list_templates` 5张关系表进行管理，完全贴合数据实体。
- **兼容与迁移**: 保留了对原有 `localStorage` 数据的向下兼容与自动数据迁移功能。有“...”菜单按钮，点击展开包含：置顶、创建副本、保存为模板、删除。** | 核心交互 |
| FR12 | **当当前笔记内容为空时，笔记内容第一行展示完整的提示文本：“记录你的想法，或 使用模板”。其中“使用模板”可点击，点击后弹出“笔记模板”弹窗。** | v1.2 完善描述 |
| FR13 | **点击“使用模板”，弹出“笔记模板”弹窗，提供多种预设排版（如会议纪要、阅读笔记、周报等）供用户选择。** | 核心交互 |
| FR14 | **删除清单时，使用自定义的悬浮气泡确认框进行二次确认。删除笔记和分组不需要确认，直接删除。** | v1.4 修改 |
| FR15 | **在侧边栏复制清单时，必须将其内部包含的所有笔记数据也一并复制。** | v1.3 新增 |
| FR16 | **侧边栏的清单支持鼠标拖拽移动（Drag & Drop）来重新排序。文件夹内的清单可以拖出文件夹，文件夹外的清单也可以拖进文件夹内，并自动更新对应的 folderId 值。** | v1.4 修改 |
| FR17 | **鼠标移动到清单内容区的笔记列表项时，笔记右侧浮现下拉菜单按钮。点击后弹出菜单：置顶、移动到（二级菜单，展示其他清单，支持搜索）、创建副本、删除。** | v1.3 新增 |
| FR18 | **新建的清单默认无分组，归入【未分组】。未分组不可修改组名、不可删除。**<br/>- 点击右上角“...”创建分组。<br/>- **分组栏最右侧显示该分组下的笔记数量，鼠标移动到分组时，数字变为下拉菜单按钮（包含重命名、删除）。**<br/>- **删除分组：直接删除分组本身且无确认提示，原笔记自动归入【未分组】。**<br/>- 分组支持折叠和展开。 | v1.4 修改 |
| FR19 | **笔记支持拖拽排序（通过新增连续整数型字段），并支持拖拽改变所属分组（改变 groupId）。** | v1.4 新增 |
| FR20 | **鼠标移动到各个笔记模板上时，hover时右上角出现编辑和删除的按钮。支持点击以编辑和删除对应的笔记模板。** | v1.5 新增 |
| FR21 | **应用笔记模板时，系统只修改笔记内容，不修改当前的笔记标题。笔记模板的标题只是表明这个笔记模板的大览标识。** | v1.5 新增 |
| FR22 | **当拖动清单悬浮在某个文件夹上方时，该文件夹应有视觉反馈（如高亮背景），表示松手后将移入该文件夹。** | v1.6 新增 |
| FR23 | **当拖动笔记悬浮在某个分组上方时，该分组应有同样的视觉反馈。** | v1.6 新增 |

### 4.3 非功能需求
- **易用性 (Usability)：** 侧边栏 Hover 菜单响应需迅速；右侧抽屉滑出动画需流畅平滑，抽屉外点击关闭符合用户直觉。
- **UI/UX：** 需高度还原设计稿，保持添加/编辑弹窗（清单、文件夹）的样式一致性；空白引导页中文案的点击区域需明显（如添加下划线或高亮颜色）。

## 5. 设计与用户体验

### 5.1 设计原则
- **上下文内操作：** 尽可能在用户光标悬停的地方提供相关操作（Hover 菜单），减少鼠标移动距离。
- **平滑过渡：** 笔记编辑采用抽屉模式而非页面跳转，帮助用户保持在当前清单上下文中的感知。
- **统一性：** “添加”与“编辑”的弹窗组件应尽可能复用，保证用户体验的一致性。

### 5.2 页面结构与交互流程
- **页面布局：** 
  - **左侧边栏**：清单导航，支持清单拖拽排序。
  - **中/右侧内容区**：顶部为笔记快捷输入框，下方为清单内的笔记列表（按分组展示，默认为无分组/未分组）。
  - **右侧抽屉 (Drawer)**：点击笔记时浮现于最右侧或覆盖内容区右半部，用于笔记编辑。点击其外部遮罩层关闭。
- **交互流程（更新）：**
  1. 笔记移动与排序：支持直接拖拽笔记以调整顺序或将其移入其他分组。也可通过 Hover 菜单将笔记跨清单转移。
  2. 分组管理：未分组不可改名或删除。分组标题最右侧显示笔记数，Hover 时变为下拉菜单。删除分组时无需确认，笔记自动归入“未分组”。
  3. 清单拖拽：清单支持在侧边栏中自由拖拽排序，支持拖出或拖入文件夹。

### 5.3 信息架构
- 侧边栏
  - 文件夹 (Hover: 添加清单(带默认文件夹)/编辑文件夹/置顶)
    - 清单 (右侧显示笔记数, Hover 变为菜单: 编辑/置顶/复制/删除, 支持拖拽进出文件夹)
- 主内容区
  - 顶部操作区: [新建笔记输入框...] / 更多操作“...” (创建分组)
  - 分组 A (最右侧显示笔记数, Hover 变为菜单: 重命名/删除, 可折叠)
    - 笔记项 1 (支持拖拽排序和改组, Hover: 置顶/移动到/创建副本/删除)
  - [未分组] (不可重命名/删除)
    - 笔记项 2

## 6. 技术规格说明建议

### 6.1 前端组件划分
- `ListLayout`, `Sidebar`, `AddListModal`, `FolderModal`
- **新增/更新组件**:
  - `HoverContextMenu`: 通用的 Hover 更多操作下拉菜单组件。
  - `ConfirmBubble`: 悬浮确认气泡框组件，用于替换 `window.confirm`。
  - `NoteGroup`: 清单内容区的分组容器组件。
  - `NoteItem`: 独立的笔记项组件，包含 Hover 时的下拉菜单和移动至搜索面板。
  - `NoteList`: 清单主内容区的笔记列表组件。
  - `NoteDrawer`: 右侧滑出的抽屉组件。

### 6.2 数据模型定义 (参考)
- **List Entity**: 
  - `id`: string, `name`: string, `folderId`: string | null, `isPinned`: boolean
  - `sortOrder`: number (连续型整数，用于拖拽排序)
- **NoteGroup Entity** (新增):
  - `id`: string
  - `listId`: string
  - `name`: string
  - `sortOrder`: number
- **Note Entity**:
  - `id`: string
  - `listId`: string
  - `groupId`: string | null (归属的分组，null 为未分组)
  - `sortOrder`: number (连续型整数，用于拖拽排序)
  - `title`: string
  - `content`: string
  - `isPinned`: boolean
  - `createdAt`, `updatedAt`: timestamp
- **Template Entity**:
  - `id`: string
  - `name`: string
  - `content`: string

### 6.3 数据持久化架构 (v1.6 新增)
清单模块的数据采用 **Rust + TiDB 直连** 的方案进行持久化同步。
- **架构**: 
  - 前端采用 React `useState` 进行内存状态管理，确保所有的 CRUD 操作具有 `0ms` 的 UI 响应极速体验（乐观更新）。
  - 后端采用 Rust `sqlx` 搭配 MySQL 协议直连远端 TiDB，提供稳定的结构化存储。
- **前后端通信**: 使用 Tauri 进程间通信机制 (IPC Invoke)。每次修改都会触发异步 API 进行数据持久化。
- **高频输入防抖 (Debouncing)**: 对高频次的编辑引入 `500ms` 的延迟防抖，对低频次的编辑引入 `300ms` 的防抖，避免极短时间内触发过多的 IPC 调用和数据库写入操作。
- **数据表设计**: 所有数据分为 `list_folders`、`list_lists`、`list_note_groups`、`list_notes`、`list_templates` 5张关系表进行管理，完全贴合数据实体。
- **兼容与迁移**: 保留了对原有 `localStorage` 数据的向下兼容与自动数据迁移功能。
